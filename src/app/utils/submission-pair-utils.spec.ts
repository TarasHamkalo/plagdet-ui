import { SubmissionPairUtils } from "./submission-pair-utils";

describe("SubmissionPairUtils", () => {
  it("should create an instance", () => {
    expect(new SubmissionPairUtils()).toBeTruthy();
  });
});
