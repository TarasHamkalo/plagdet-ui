import { TextOverflowScrollDirective } from './text-overflow-scroll.directive';

describe('TextOverflowScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new TextOverflowScrollDirective();
    expect(directive).toBeTruthy();
  });
});
